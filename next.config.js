/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',  // Export as static files for Capacitor
    distDir: 'out',    // Set the output directory to 'out'
  };
  
  module.exports = nextConfig;
  